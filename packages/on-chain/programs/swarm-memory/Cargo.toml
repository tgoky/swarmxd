[package]
name = "swarm-memory"
version = "0.1.0"
description = "Swarm Conductor on-chain memory — immutable hive-mind decision log"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "swarm_memory"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
